{
  "name": "kethcompany-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "sites/*",
    "admin"
  ],
  "scripts": {
    "dev:test-faq": "npm run dev --workspace=sites/test-faq",
    "dev:admin": "npm run dev --workspace=admin",
    "build:test-faq": "npm run build --workspace=sites/test-faq",
    "build:admin": "npm run build --workspace=admin"
  }
}

